{
  "name": "bachelor-node",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev": "cross-env NODE_ENV=development nodemon --exec babel-node -- ./src",
    "debug": "cross-env NODE_ENV=development babel-node %NODE_DEBUG_OPTION% -- ./bin/www",
    "cluster": "cross-env NODE_ENV=development nodemon --exec babel-node -- ./performance/clustering",
    "clusterdebug": "cross-env NODE_ENV=development nodemon --exec babel-node %NODE_DEBUG_OPTION% -- ./performance/clustering",
    "profiling": "cross-env NODE_ENV=development babel-node --prof ./bin/www",
    "build": "cross-env NODE_ENV=production babel bin/www -s -D -d dist",
    "start": "cross-env NODE_ENV=production nodemon --exec babel-node -- ./bin/www",
    "dashboard": "cross-env NODE_ENV=development nodemon --exec 'nodejs-dashboard babel-node' -- ./bin/www"
  },
  "dependencies": {
    "@stdlib/stdlib": "0.0.40",
    "express": "~4.16.0",
    "kdbush": "^1.0.1",
    "morgan": "^1.9.0",
    "node-fetch": "^2.2.0",
    "sharp": "^0.20.7",
    "socket.io": "^2.1.0",
    "tayden-clusterfck": "^0.7.0"
  },
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-env": "^1.7.0",
    "cross-env": "^5.2.0",
    "eslint": "^4.19.1",
    "eslint-config-airbnb-base": "^12.1.0",
    "eslint-plugin-import": "^2.14.0",
    "eslint-plugin-jsx-a11y": "^6.1.1",
    "nodemon": "^1.18.3"
  }
}
